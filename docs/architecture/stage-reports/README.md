# AOT / Rust 阶段验证报告归档

这里存放 Rust 与 Native AOT 分阶段优化过程中产生的一次性验证报告（AOT 阶段 4D–5B 与 Rust 阶段 6–7 系列）。2026-09-06 从 `docs/architecture/` 根目录移入本目录，内容保持原样，仅作过程存证，不再更新。

各阶段的当前状态、后续计划与审计口径以 [`../../rust-native-aot-roadmap.md`](../../rust-native-aot-roadmap.md) 为准；现行 ABI 契约见根目录的 `*-native-abi-*.md`（search-core 现行为 v3）。GitHub Actions 运行证据见 [`../evidence/`](../evidence/)。
