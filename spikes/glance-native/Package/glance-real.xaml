<?xml version="1.0" encoding="utf-8"?>
<!-- Ported from src/DeskBox/Controls/WidgetContents/GlanceWidgetContent.xaml for the
     batch B real-Glance slice. Verbatim: the day-item style/template shape, the glass
     surface, the compact header, CalendarView and its resource overrides. Removed:
     x:Class wiring, code-behind event attributes, image/immersive/editorial layers
     and the photo action bar (they belong to image services not in this slice).
     CONTRACT FINDING: runtime XamlReader.Load cannot resolve package-local custom
     types (no XAML metadata provider for a native DLL's namespace), so converters
     are impossible here - the bindable models pre-compute Visibility/FontWeight
     instead. The CalendarViewDayItemChanging handler is wired in package code. -->
<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="RootGrid"
    Padding="20,22,20,18"
    Background="Transparent">

    <!-- CONTRACT FINDING: ThemeResource lookups for WinUI control-theme keys
         (the *FillColor* brushes) do not reach runtime-parsed package XAML even
         with XamlControlsResources merged in the host Application; only
         platform-level keys resolve. Runtime-loaded package XAML must therefore
         be resource-self-contained. Values below approximate the Fluent 2 dark
         tokens for slice rendering (single theme; production needs theme
         awareness designed in batch C). -->
    <Grid.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#C5FFFFFF" />
            <SolidColorBrush x:Key="TextFillColorDisabledBrush" Color="#5DFFFFFF" />
            <SolidColorBrush x:Key="TextOnAccentFillColorPrimaryBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="SubtleFillColorSecondaryBrush" Color="#0FFFFFFF" />
            <SolidColorBrush x:Key="SubtleFillColorTertiaryBrush" Color="#0AFFFFFF" />
            <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="{ThemeResource SystemAccentColor}" />
            <Color x:Key="SolidBackgroundFillColorBase">#202020</Color>
            <AcrylicBrush
                x:Key="GlanceCalendarAcrylicBrush"
                FallbackColor="{ThemeResource SolidBackgroundFillColorBase}"
                TintColor="{ThemeResource SolidBackgroundFillColorBase}" />
        <Style x:Key="GlanceCalendarDayItemStyle" TargetType="CalendarViewDayItem">
            <Setter Property="MinWidth" Value="34" />
            <Setter Property="MinHeight" Value="24" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="UseSystemFocusVisuals" Value="True" />
            <Setter Property="FocusVisualMargin" Value="-2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarViewDayItem">
                        <!-- CalendarView keeps native navigation and date semantics;
                             this compact template owns the two-line day typography. -->
                        <Grid MinWidth="34" MinHeight="24" Background="Transparent">
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Opacity="{Binding Tag.PrimaryOpacity, RelativeSource={RelativeSource TemplatedParent}}"
                                Spacing="0"
                                Visibility="{Binding Tag.NonTodayVisibility}">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                    LineHeight="13"
                                    LineStackingStrategy="BlockLineHeight"
                                    Text="{Binding Tag.DayText, RelativeSource={RelativeSource TemplatedParent}}"
                                    Typography.NumeralAlignment="Tabular" />
                                <TextBlock
                                    MaxWidth="32"
                                    HorizontalAlignment="Center"
                                    FontSize="7.5"
                                    FontWeight="{Binding Tag.SecondaryFontWeight}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    LineHeight="10"
                                    LineStackingStrategy="BlockLineHeight"
                                    MaxLines="1"
                                    Opacity="{Binding Tag.SecondaryOpacity, RelativeSource={RelativeSource TemplatedParent}}"
                                    Text="{Binding Tag.SecondaryText, RelativeSource={RelativeSource TemplatedParent}}"
                                    TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding Tag.SecondaryVisibility}" />
                            </StackPanel>
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Spacing="0"
                                Visibility="{Binding Tag.TodayVisibility}">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    FontSize="11"
                                    FontWeight="SemiBold"
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                    LineHeight="13"
                                    LineStackingStrategy="BlockLineHeight"
                                    Text="{Binding Tag.DayText, RelativeSource={RelativeSource TemplatedParent}}"
                                    Typography.NumeralAlignment="Tabular" />
                                <TextBlock
                                    MaxWidth="32"
                                    HorizontalAlignment="Center"
                                    FontSize="7.5"
                                    FontWeight="{Binding Tag.SecondaryFontWeight}"
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                    LineHeight="10"
                                    LineStackingStrategy="BlockLineHeight"
                                    MaxLines="1"
                                    Opacity="0.86"
                                    Text="{Binding Tag.SecondaryText, RelativeSource={RelativeSource TemplatedParent}}"
                                    TextTrimming="CharacterEllipsis"
                                    Visibility="{Binding Tag.SecondaryVisibility}" />
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </Grid.Resources>

    <Grid
        x:Name="CalendarLayoutRoot"
        Visibility="{Binding CalendarLayoutVisibility}">

        <StackPanel
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Spacing="-1"
            Visibility="{Binding IsExpandedVisibility}">
            <TextBlock
                FontFamily="{Binding TimeFontFamily}"
                FontSize="{Binding CompactTimeFontSize}"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                Text="{Binding TimeText}"
                Typography.NumeralAlignment="Tabular"
                Visibility="{Binding ShowTimeVisibility}" />
            <StackPanel Orientation="Horizontal" Spacing="7">
                <TextBlock FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Text="{Binding DateText}" Visibility="{Binding ShowDateVisibility}" />
                <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding WeekdayText}" Visibility="{Binding ShowWeekdayVisibility}" />
            </StackPanel>
        </StackPanel>

        <Border
            x:Name="CalendarGlassSurface"
            Height="{Binding CalendarPanelHeight}"
            HorizontalAlignment="Center"
            Margin="-6,0,-6,0"
            MaxWidth="{Binding CalendarPanelMaxWidth}"
            VerticalAlignment="Bottom"
            Width="{Binding CalendarPanelWidth}"
            CornerRadius="{Binding CalendarCornerRadius}">
            <Grid>
                <Border
                    x:Name="CalendarMaterialSurface"
                    Background="{StaticResource GlanceCalendarAcrylicBrush}"
                    CornerRadius="{Binding CalendarCornerRadius}"
                    IsHitTestVisible="False" />
                <Grid Margin="8,0,8,4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid
                        Grid.Row="0"
                        Height="27"
                        Margin="13,7,6,0"
                        Visibility="{Binding IsCompactVisibility}">
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            FontFamily="{Binding TimeFontFamily}"
                            FontSize="{Binding CalendarCompactTimeFontSize}"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            LineHeight="26"
                            LineStackingStrategy="BlockLineHeight"
                            Margin="0,5,0,-5"
                            Text="{Binding TimeText}"
                            Typography.NumeralAlignment="Tabular"
                            Visibility="{Binding ShowTimeVisibility}" />
                        <StackPanel Margin="0,5,0,-5" HorizontalAlignment="Right" VerticalAlignment="Top" Spacing="0">
                            <TextBlock HorizontalAlignment="Right" FontSize="10" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" LineHeight="12" LineStackingStrategy="BlockLineHeight" Text="{Binding CompactCalendarDateText}" Visibility="{Binding ShowDateVisibility}" />
                            <TextBlock HorizontalAlignment="Right" FontSize="9.5" Foreground="{ThemeResource TextFillColorSecondaryBrush}" LineHeight="11" LineStackingStrategy="BlockLineHeight" Text="{Binding WeekdayText}" Visibility="{Binding ShowWeekdayVisibility}" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1">
                        <CalendarView
                            x:Name="NativeCalendarView"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            CalendarItemBackground="Transparent"
                            CalendarItemBorderBrush="Transparent"
                            CalendarItemBorderThickness="0"
                            CalendarItemCornerRadius="6"
                            CalendarItemForeground="{ThemeResource TextFillColorPrimaryBrush}"
                            CalendarItemHoverBackground="{ThemeResource SubtleFillColorSecondaryBrush}"
                            CalendarItemPressedBackground="{ThemeResource SubtleFillColorTertiaryBrush}"
                            CalendarViewDayItemStyle="{StaticResource GlanceCalendarDayItemStyle}"
                            DayItemFontSize="0.1"
                            DayItemMargin="0"
                            DayOfWeekFormat="{}{dayofweek.abbreviated(1)}"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            HorizontalDayItemAlignment="Center"
                            IsGroupLabelVisible="False"
                            IsOutOfScopeEnabled="True"
                            IsTodayHighlighted="True"
                            NumberOfWeeksInView="6"
                            OutOfScopeBackground="Transparent"
                            OutOfScopeForeground="{ThemeResource TextFillColorDisabledBrush}"
                            SelectionMode="None"
                            TodayBackground="{ThemeResource AccentFillColorDefaultBrush}"
                            TodayFontWeight="SemiBold"
                            TodayForeground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            VerticalDayItemAlignment="Center">
                            <CalendarView.Resources>
                                <x:Double x:Key="CalendarViewHeaderNavigationButtonFontSize">13</x:Double>
                                <x:Double x:Key="CalendarViewNavigationButtonFontSize">10</x:Double>
                                <Thickness x:Key="CalendarViewDayItemMargin">0</Thickness>
                                <Thickness x:Key="CalendarViewWeekDayMargin">0</Thickness>
                                <Thickness x:Key="CalendarViewWeekDayPadding">1,4,1,4</Thickness>
                                <Thickness x:Key="CalendarViewMonthYearItemMargin">3</Thickness>
                                <Thickness x:Key="CalendarViewHeaderNavigationButtonPadding">8,6,8,6</Thickness>
                                <Thickness x:Key="CalendarViewNavigationButtonPadding">9,6,9,6</Thickness>
                                <Thickness x:Key="CalendarViewHeaderNavigationButtonMargin">4,2,2,2</Thickness>
                                <Thickness x:Key="CalendarViewNavigationPreviousButtonMargin">2,2,1,2</Thickness>
                                <Thickness x:Key="CalendarViewNavigationNextButtonMargin">1,2,4,2</Thickness>
                            </CalendarView.Resources>
                        </CalendarView>
                        <TextBlock
                            x:Name="TraditionalCalendarTitlePresenter"
                            Height="29"
                            Margin="98,0,70,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"
                            FontSize="8"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            IsHitTestVisible="False"
                            LineHeight="29"
                            LineStackingStrategy="BlockLineHeight"
                            MaxLines="1"
                            Opacity="0.68"
                            Text="{Binding TraditionalCalendarTitle}"
                            TextAlignment="Center"
                            TextTrimming="CharacterEllipsis"
                            Visibility="Visible" />
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Grid>
